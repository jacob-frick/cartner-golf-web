import { makeStyles } from '@material-ui/core/styles';
const sentReqDisplayStyles = makeStyles((theme) => ({
  root: {
    flexGrow: 1
  },
  listStyle: {
    display: 'contents'
  }
}));

export default sentReqDisplayStyles